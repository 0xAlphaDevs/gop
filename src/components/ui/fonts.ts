import { Cinzel } from "next/font/google";

export const cinzel = Cinzel({ weight: "400", subsets: ["latin"] });
export const cinzel500 = Cinzel({ weight: "500", subsets: ["latin"] });
export const cinzel600 = Cinzel({ weight: "600", subsets: ["latin"] });
export const cinzel900 = Cinzel({ weight: "900", subsets: ["latin"] });
